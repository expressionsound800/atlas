# Contributing

This distribution is initially a deterministic release surface. Atlas Core and
Atlas Graph implementation changes must be made in their canonical component
repositories and exported from reviewed revisions. Do not edit generated files
under `core/` or `graph/` as a second implementation authority.

Before accepting outside implementation contributions, maintainers must either
move canonical source authority to a public repository or document a formal
contribution-import workflow.
